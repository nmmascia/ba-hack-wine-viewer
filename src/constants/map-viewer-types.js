/* eslint-disable import/prefer-default-export */

export const MAP_VIEWER_TYPES = Object.freeze({
  HEAT_MAP: {
    key: 'HEAT_MAP',
    name: 'Heat map',
  },
  WINE_PINS: {
    key: 'WINE_PINS',
    name: 'Wine Pins',
  },
});

export const MAP_VIEWER_KEYS = Object.keys(MAP_VIEWER_TYPES);
